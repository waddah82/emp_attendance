from setuptools import setup, find_packages

setup(
    name="emp_attendance",
    version="0.0.1",
    description="PWA Employee Attendance for Frappe",
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
    install_requires=[],
)
